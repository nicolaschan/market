// Generated by CoffeeScript 1.10.0
(function() {
  var select, selectWithQueryString;

  select = function(fields, allValues) {
    var addToResult, field, i, len, result;
    if ((fields != null) && fields.length > 0) {
      result = {};
      addToResult = function(field) {
        if (allValues[field] != null) {
          return result[field] = allValues[field];
        }
      };
      for (i = 0, len = fields.length; i < len; i++) {
        field = fields[i];
        addToResult(field);
      }
      return result;
    } else {
      return allValues;
    }
  };

  selectWithQueryString = function(query, allValues) {
    var fields;
    fields = query != null ? query.split(',') : [];
    return select(fields, allValues);
  };

  module.exports.select = select;

  module.exports.selectWithQueryString = selectWithQueryString;

}).call(this);
