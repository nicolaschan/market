# Random-to

```
var random = require('random-to');

var w = random.from0upto(10); // 0 - 9
var x = random.from0to(10); // 0 - 10
var y = random.from1to(10); // 1 - 10
var z = random.from0to(-10); // -10 - 0
```
